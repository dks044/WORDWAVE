import React from "react"
import VocaBookListContainer from "../containers/voca/VocaBookListContainer";
const VocaBookListPage = () =>{
  return(
    <VocaBookListContainer />
  )
}

export default React.memo(VocaBookListPage);
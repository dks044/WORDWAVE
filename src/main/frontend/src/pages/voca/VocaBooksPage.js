import React from "react";
import VocaBooksContainer from "../../containers/voca/VocaBooksContainer"

const VocaBooksPage = () => {
  return (
    <VocaBooksContainer />
  )
}

export default React.memo(VocaBooksPage);